name: background_issues_example
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  contextual_menu: ^0.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

dependency_overrides:
  contextual_menu:
    git:
      url: https://github.com/jmatth/contextual_menu.git
      ref: cc-custom
  menu_base:
    git:
      url: https://github.com/jmatth/menu_base.git
      ref: cc-custom

flutter:
  uses-material-design: true